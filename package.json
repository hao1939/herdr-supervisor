{
  "name": "herdr-supervisor",
  "version": "0.1.0",
  "private": true,
  "description": "One Pi agent that supervises existing Herdr workers against explicit goals, without adding a second task system.",
  "license": "MIT",
  "author": "Hao Yuan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hao1939/herdr-supervisor.git"
  },
  "homepage": "https://github.com/hao1939/herdr-supervisor#readme",
  "bugs": {
    "url": "https://github.com/hao1939/herdr-supervisor/issues"
  },
  "keywords": [
    "herdr",
    "codex",
    "agent-supervision",
    "pi-coding-agent"
  ],
  "type": "module",
  "bin": {
    "herdr-supervisor": "bin/herdr-supervisor.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --experimental-strip-types --check extension.ts && node --experimental-strip-types --check container/pi-extension.ts && node --check bin/herdr-supervisor.js && node --check src/global-review.js && node --check src/goal-registry.js && node --check src/goal-store.js && node --check src/herdr-client.js && node --check src/observation.js && node --check src/review-turn.js && node --check src/supervision.js && sh -n bin/container-entrypoint.sh && sh -n container/bin/codex && sh -n container/bin/pi"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2"
  }
}
