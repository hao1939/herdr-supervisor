name: E2E

on:
  workflow_dispatch:
  schedule:
    - cron: "23 9 * * 1" # Monday 9:23 UTC

concurrency:
  group: e2e
  cancel-in-progress: true

jobs:
  e2e:
    if: github.repository == 'hao1939/herdr-supervisor'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: herdr-supervisor:e2e

      - name: Start Herdr with supervisor
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ -z "$ANTHROPIC_API_KEY" ] || [ -z "$OPENAI_API_KEY" ]; then
            echo "::error::ANTHROPIC_API_KEY and OPENAI_API_KEY secrets are required"
            exit 1
          fi

          docker run -d --name herdr-e2e \
            -e ANTHROPIC_API_KEY \
            -e OPENAI_API_KEY \
            -e HERDR_SUPERVISOR_CODEX_FULL_ACCESS=1 \
            herdr-supervisor:e2e \
            herdr server

          sleep 3
          docker exec herdr-e2e herdr status

      - name: Start Pi supervisor
        run: |
          # Start Pi with the supervisor extension in a Herdr pane
          docker exec herdr-e2e \
            herdr agent start supervisor --kind pi --pane w0:p0 -- \
              -e /opt/herdr-supervisor/src/extension.ts \
              --no-builtin-tools

          # Wait for Pi to become interactive
          docker exec herdr-e2e \
            herdr agent wait w0:p0 --until idle --timeout 30000

          echo "Pi supervisor is running"

      - name: Create a test goal
        run: |
          # Ask the supervisor to create a trivial goal
          docker exec herdr-e2e \
            herdr agent prompt w0:p0 \
              "Create a supervised goal: objective is 'Write hello.txt containing hello world', acceptance is 'hello.txt exists with the text hello world', working directory is /app, place it in a new tab labeled test-worker."

          # Wait for the supervisor to process and start the worker
          docker exec herdr-e2e \
            herdr agent wait w0:p0 --until idle --timeout 120000

          # Verify a worker was created
          workers=$(docker exec herdr-e2e herdr agent list 2>&1)
          echo "$workers"

          if echo "$workers" | grep -q "codex"; then
            echo "Codex worker started successfully"
          else
            echo "::error::No Codex worker was created"
            exit 1
          fi

          goal_states=$(docker exec herdr-e2e \
            find /home/node/.local/state/herdr-supervisor/goals \
              -mindepth 2 -maxdepth 2 -name current.json -type f)
          goal_count=$(echo "$goal_states" | sed '/^$/d' | wc -l)
          if [ "$goal_count" -ne 1 ]; then
            echo "::error::Expected one canonical goal checkpoint, found $goal_count"
            exit 1
          fi
          echo "GOAL_STATE_PATH=$goal_states" >> "$GITHUB_ENV"

      - name: Wait for worker to produce the deliverable
        run: |
          deadline=$((SECONDS + 180))
          while [ "$SECONDS" -lt "$deadline" ]; do
            if docker exec herdr-e2e sh -c \
              'test -f /app/hello.txt && grep -qF "hello world" /app/hello.txt'; then
              docker exec herdr-e2e cat /app/hello.txt
              echo "Deliverable matches the acceptance criterion"
              exit 0
            fi
            sleep 5
          done

          echo "::error::Worker did not produce hello.txt containing 'hello world' within 3 minutes"
          docker exec herdr-e2e sh -c 'test ! -e /app/hello.txt || cat /app/hello.txt' || true
          exit 1

      - name: Wait for the supervisor to accept the goal
        run: |
          # Observe the authoritative checkpoint. Terminal scrollback is not
          # proof because it can contain output from an earlier prompt.
          deadline=$((SECONDS + 180))
          while [ "$SECONDS" -lt "$deadline" ]; do
            if docker exec herdr-e2e node -e '
              const state = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
              process.exit(state.terminal?.state === "accepted" ? 0 : 1);
            ' "$GOAL_STATE_PATH"; then
              echo "Supervisor recorded canonical goal acceptance"
              exit 0
            fi
            sleep 5
          done

          echo "::error::Supervisor never accepted the goal within 3 minutes of the deliverable appearing"
          docker exec herdr-e2e cat "$GOAL_STATE_PATH" || true
          exit 1

      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== Herdr status ==="
          docker exec herdr-e2e herdr status 2>&1 || true
          echo "=== Agent list ==="
          docker exec herdr-e2e herdr agent list 2>&1 || true
          echo "=== Supervisor output ==="
          docker exec herdr-e2e herdr agent read w0:p0 --lines 50 2>&1 || true
          echo "=== Container logs ==="
          docker logs herdr-e2e 2>&1 | tail -50 || true

      - name: Cleanup
        if: always()
        run: docker rm -f herdr-e2e 2>/dev/null || true
